<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cmpe277.downloadmanager">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver android:name=".MainActivity$ReceiveDownloadMessage">
            <intent-filter>
                <action android:name="com.cmpe277.downloadmanager.message" />
            </intent-filter>
        </receiver>

        <service android:name=".DownloadService">
            <intent-filter>
                <action android:name="com.cmpe277.downloadmanager.DownloadService" />
            </intent-filter>
        </service>
        <service
            android:name=".PreDownloadService"
            android:enabled="false"
            android:exported="false" />
        <service
            android:name=".PreDownloadIntentService"
            android:exported="false"></service>
    </application>

</manifest>